# Amarillo Dependencies

# Core dependencies
openai>=1.60.0              # OpenAI SDK with GPT-5.2 and Codex support
python-dotenv>=1.0.0        # Environment configuration
numpy>=1.24.0               # Used for soft scoring and array operations

# Development dependencies (optional)
pytest>=7.0.0               # Testing framework

# Optional: OpenAI Agents SDK for MCP server integration
# pip install openai-agents
# openai-agents>=0.5.0
